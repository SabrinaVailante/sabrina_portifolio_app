import 'package:flutter/material.dart';

class MySkilsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Aperfeiçoando...'),
      ),
      body: Center(
        child: Text('Aperfeiçoando...'),
      ),
    );
  }
}